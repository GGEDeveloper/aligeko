{
  "name": "alitools-b2b",
  "version": "1.0.0",
  "description": "AliTools B2B E-commerce Platform",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "install:all": "npm install && cd server && npm install && cd ../client && npm install",
    "build:client": "cd client && npm install && npm run build",
    "build:server": "cd server && npm install && npm run build",
    "build": "npm run build:client && npm run build:server",
    "start": "node index.js",
    "dev": "concurrently \"cd server && npm run dev\" \"cd client && npm run dev\"",
    "test": "cd server && npm test",
    "steps": "node tasks/step-tracker.js",
    "steps:report": "node -e \"try { import('./tasks/step-tracker.js').then(tracker => { const tasks = tracker.parseStepsFile(); tracker.displayProgressReport(tasks); }).catch(e => console.error('Error loading step tracker:', e)); } catch (err) { console.error('Error running steps report:', err); }\"",
    "check:env": "node scripts/env-check.js",
    "predeploy": "npm run check:env",
    "deploy": "bash ./deploy.sh",
    "deploy:vercel": "vercel --prod",
    "vercel:logs": "vercel logs",
    "vercel:list": "vercel list",
    "deploy:fix": "node deploy-fix.js",
    "test:deployment": "node test-deployment.js",
    "db:check-storage": "node server/src/scripts/storage-management.js check",
    "db:cleanup": "node server/src/scripts/storage-management.js cleanup",
    "db:backup": "node server/src/scripts/storage-management.js backup",
    "db:storage-test": "node server/src/scripts/test-storage-management.js all",
    "db:import-xml": "node server/src/scripts/direct-import-xml.js"
  },
  "keywords": [
    "ecommerce",
    "b2b",
    "tools",
    "alitools"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "chalk": "^5.3.0",
    "concurrently": "^8.0.1",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.21.2",
    "fast-xml-parser": "^5.2.2",
    "helmet": "^7.1.0",
    "node-fetch": "^3.3.2",
    "pg": "^8.15.6",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.37.7"
  },
  "devDependencies": {
    "node-fetch": "^2.7.0"
  }
}
